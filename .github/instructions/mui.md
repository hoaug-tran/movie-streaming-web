## Use the mui-mcp server to answer any MUI questions

- call the "useMuiDocs" tool to fetch the docs of the package relevant to the question
- call the "fetchDocs" tool to fetch any additional docs if needed using only the URLs present in the returned content
- repeat until enough official Material UI documentation is gathered
- answer using the fetched official docs and examples
